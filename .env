# =============================================================================
# Isaac Sim / Lab Development Container - Environment Configuration
# =============================================================================

# --- Isaac Sim / Lab versions ---
ISAAC_SIM_VERSION=5.1.0
ISAAC_SIM_IMAGE=nvcr.io/nvidia/isaac-sim
ISAAC_LAB_VERSION=v2.3.2
ISAAC_LAB_REPO=https://github.com/isaac-sim/IsaacLab.git

# --- EULA acceptance (set to "Y" to accept NVIDIA license) ---
ACCEPT_EULA=Y
PRIVACY_CONSENT=Y

# --- Host user mapping (auto-detected at build time via docker compose) ---
HOST_UID=1000
HOST_GID=1000
HOST_USER=developer

# --- Project paths ---
# The host directory to mount as workspace (default: current directory)
PROJECT_DIR=.
# Container-side workspace path
WORKSPACE=/workspace

# --- Isaac Sim cache (persisted on host to speed up subsequent runs) ---
ISAAC_CACHE_DIR=~/.cache/isaac-sim

# --- Display / GUI forwarding ---
DISPLAY=${DISPLAY:-:1}
